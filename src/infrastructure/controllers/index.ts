import { processCollection, saveCollection, uploadCollection } from './collection'

export const controllers = {
  collection: {
    uploadCollection,
    processCollection,
    saveCollection
  },
  fileSystem: {

  }
}
