import { getCollectionJson, processCollection, saveCollection } from './collection'

export const controllers = {
  collection: {
    getCollectionJson,
    processCollection,
    saveCollection
  },
  fileSystem: {

  }
}
