import { getCollection, processCollection, saveCollection } from './collection'

export const controllers = {
  collection: {
    getCollection,
    processCollection,
    saveCollection
  },
  fileSystem: {

  }
}
