import { Collection } from '../models'

export const addCollection = (collection: Collection, format: string): string => 'collectionId'

export const getCollection = (collectionId: string, format: string): Collection => <Collection>{}
